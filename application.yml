spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rentdb?serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: dzikdzik
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

